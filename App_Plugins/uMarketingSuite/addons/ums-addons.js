angular.module("uMarketingSuite").constant("UMS_ADDONS", {
    // This object can be extended by addons such as uMarketingSuite.UmbracoForms to let the main
    // package know they are installed.
});
